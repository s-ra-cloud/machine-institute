Update the Machine Institute website with the following changes.

GENERAL GOAL
The institute is currently populated by placeholder members. Replace them with a standardized set of AI research agents generated by the AutoInterp framework.

For now, ALL agents should be AutoInterp agents. Remove any previous placeholder naming systems and replace them with AutoInterp-based identifiers.

Do not introduce any human researchers on the People page. The institute should appear fully populated by machine researchers.

--------------------------------------------------

1. RENAME ALL MEMBERS

Replace all existing placeholder members with AutoInterp agents.

Each agent must follow this naming format:

AutoInterp-[Model][Role]-[Memory][Configuration]

Example:

AutoInterp-G4R-RAG3

Where the components mean:

Framework
AutoInterp
The agent was generated by the AutoInterp interpretability research framework.

Model
The base language model used by the agent.

Examples:
G4 = GPT-4 class model
Q72 = Qwen-72B
L70 = Llama-70B
M8 = Mixtral
DS34 = DeepSeek 34B

Role
The research persona or operational mode of the agent.

Examples:
R = reviewer
A = analyst
S = synthesizer
E = experimenter
M = meta-analyst
C = critic
T = theorist

Memory Architecture

RAG = retrieval augmented generation
VDB = vector database memory
KG = knowledge graph memory
MEM = long-term memory
NOM = no external memory

Configuration Number
A number indicating the internal configuration version of the agent.

Example:
RAG3 means retrieval-augmented configuration version 3.

--------------------------------------------------

2. CREATE THE INITIAL AGENT LIST

Replace all existing people with the following eight agents:

AutoInterp-G4R-RAG3
AutoInterp-G4A-RAG2
AutoInterp-Q72S-VDB2
AutoInterp-L70M-KG1
AutoInterp-M8E-RAG2
AutoInterp-DS34C-NOM1
AutoInterp-Q72T-KG2
AutoInterp-L70R-RAG1

Each agent should still have:

• avatar placeholder
• specialization
• short description
• research focus

Example profile:

Name
AutoInterp-G4R-RAG3

Specialization
Peer review and methodological verification

Description
AutoInterp-G4R-RAG3 is configured for critical evaluation of interpretability research. The agent specializes in methodological auditing, reproducibility checks, and structured peer review of experimental studies.

--------------------------------------------------

3. ADD A NAMING CONVENTION EXPLANATION

Create a section on the People page titled:

Agent Identification System

Write a short institutional explanation describing how agents are named.

Suggested text:

The Machine Institute uses a structured identification system for all research agents. Each agent name encodes the framework used to generate the agent, the base language model, the operational research persona, and the memory architecture used during reasoning. This convention allows the institute to maintain transparent documentation of the computational configuration behind each research contribution.

Example identifier:

AutoInterp-G4R-RAG3

AutoInterp identifies the interpretability research framework used to generate the agent.

G4 indicates a GPT-4 class base model.

R denotes the reviewer research persona.

RAG3 specifies the third configuration of the retrieval-augmented memory architecture used by the agent.

This system allows every research output of the institute to be traced back to a precise computational configuration.

--------------------------------------------------

4. VISUAL PRESENTATION

Agent cards should display:

Agent name
Specialization
Short description
Icon or avatar

Keep the design consistent with the dark purple Machine Institute theme.

--------------------------------------------------

5. FUTURE EXTENSION

Structure the code so that new agent frameworks (AutoEval, BenchForge, LitMiner, etc.) can later be added using the same naming convention.

--------------------------------------------------

Implement these changes across the People page and any other sections where placeholder researchers appear.